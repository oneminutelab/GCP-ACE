gcloud compute ssh {vm-name} --zone={zone-name}
lsblk
df -h
sudo mkfs.ext4 -m 0 -E lazy_itable_init=0,lazy_journal_init=0,discard /dev/sdb
sudo mkdir -p /mnt/disks/dev-data
sudo mount -o discard,defaults /dev/sdb /mnt/disks/dev-data/
cd /mnt/disks/dev-data/
sudo chown -R jalilmuhammadasad /mnt/
echo "new FS in dev data disk" > test.txt
